#include "../include/Trip.hpp"

Trip ::Trip(int _start_time_stamp, int _end_time_stamp, int _player_id, int _Trip_distance) : StartTimeStamp(_start_time_stamp),
    EndTimeStamp(_end_time_stamp),
    PlayerID(_player_id),
    TripDistance(_Trip_distance)
{
    //  nothing ...
}